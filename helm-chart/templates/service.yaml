apiVersion: v1
kind: Service
metadata:
  name: {{ include "kibana-sso-proxy.fullname" . }}
  labels:
    {{- include "kibana-sso-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kibana-sso-proxy.selectorLabels" . | nindent 4 }}
